% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sens-get.R
\name{get_sens}
\alias{get_sens}
\title{Sensitivity analysis for unmeasured confounding}
\usage{
get_sens(
  data,
  treat,
  outcome,
  time = NULL,
  instrument = NULL,
  adj_var = NULL,
  bench_var = NULL,
  method = c("lm", "cox", "dml", "iv"),
  q = 1,
  conf_level = 0.95,
  bench_args = list(k_treat = 1, k_out = NULL, bound = NULL, bound_label =
    "Manual bound"),
  evalue_args = list(rare = FALSE, confounder = c("continuous", "binary"), smd = 1,
    exposed_prev = NULL, unexposed_prev = NULL),
  dml_args = list(model = c("plm", "npm"), target = "ate", reg = "ranger", cf_folds = 5L,
    cf_reps = 1L, cf_seed = NULL, ps_trim = 0.01, dirty_tuning = TRUE, rho2 = 1,
    combine_method = "median"),
  iv_args = list(parm = c("iv", "fs", "rf"), min = TRUE),
  verbose = FALSE
)
}
\arguments{
\item{data}{A data frame holding every column named below.}

\item{treat, outcome}{Length-1 character. The treatment and outcome columns.
\code{treat} must resolve to exactly one model coefficient: numeric, or a
two-level factor.}

\item{time}{Length-1 character. Follow-up time column. Required for, and
only accepted by, \code{method = "cox"}.}

\item{instrument}{Length-1 character. Instrument column. Required for, and
only accepted by, \code{method = "iv"}.}

\item{adj_var}{Character vector of measured covariates to adjust for, or
\code{NULL}.}

\item{bench_var}{Character vector of covariates used to calibrate how strong
an unmeasured confounder would have to be, expressed as multiples of an
observed covariate. Must be a subset of \code{adj_var}. Not used by
\code{method = "cox"}.}

\item{method}{Backend selector: \code{"lm"} (sensemakr partial \eqn{R^2}),
\code{"cox"} (tipr tipping point and E-value), \code{"dml"} (dml.sensemakr) or
\code{"iv"} (iv.sensemakr).}

\item{q}{Fraction of the estimate a confounder would have to explain away
for the result to be considered overturned. \code{q = 1} asks what it takes to
reach the null. Must be 1 for \code{method = "cox"}.}

\item{conf_level}{Confidence level. The backends receive \code{1 - conf_level}
as their \code{alpha}; there is deliberately no separate \code{alpha} argument.}

\item{bench_args}{Named list controlling benchmark and manual confounding
scenarios, used by \code{method} \code{"lm"}, \code{"dml"} and \code{"iv"}.
\describe{
\item{\code{k_treat}}{Positive numeric vector. How many times as strongly as
\code{bench_var} the confounder is related to the treatment (or, for
\code{"iv"}, to the instrument). Default \code{1}.}
\item{\code{k_out}}{Positive numeric vector or \code{NULL}. Same multiple on the
outcome side. \code{NULL} (default) reuses \code{k_treat}.}
\item{\code{bound}}{Length-2 numeric in (0, 1) giving one manual scenario as
\verb{c(treatment-side R2, outcome-side R2)}, or \code{NULL} (default). Mapped
to \code{r2dz.x}/\code{r2yz.dx} for \code{"lm"}, \code{cf.d}/\code{cf.y} for \code{"dml"} and
\code{r2zw.x}/\code{r2y0w.zx} for \code{"iv"}.}
\item{\code{bound_label}}{Length-1 character labelling that manual scenario.
Default \code{"Manual bound"}.}
}}

\item{evalue_args}{Named list for \code{method = "cox"} only.
\describe{
\item{\code{rare}}{Logical, default \code{FALSE}. \code{TRUE} treats the outcome as
rare, so the hazard ratio is used directly as a risk ratio. \code{FALSE}
applies the VanderWeele-Ding square-root transform. The same switch
drives \code{tipr}'s \code{hr_correction}, which it sets to \code{!rare}.}
\item{\code{confounder}}{\code{"continuous"} (default) or \code{"binary"}, selecting
\code{\link[tipr:tip_hr]{tipr::tip_hr()}} or \code{\link[tipr:tip_hr_with_binary]{tipr::tip_hr_with_binary()}}.}
\item{\code{smd}}{Positive numeric, default \code{1}. Standardised mean difference
of a continuous confounder between treatment groups.}
\item{\code{exposed_prev},\code{unexposed_prev}}{Numeric in (0, 1) or \code{NULL}
(default). Confounder prevalence in each arm; both are required when
\code{confounder = "binary"}.}
}}

\item{dml_args}{Named list for \code{method = "dml"} only. Fields map to
\code{\link[dml.sensemakr:dml]{dml.sensemakr::dml()}} and its \code{sensemakr()} method: \code{model}
(\code{"plm"}/\code{"npm"}), \code{target} (\code{"ate"}, \code{"att"}, \code{"atu"}), \code{reg} (caret
learner, default \code{"ranger"}), \code{cf_folds} (default \code{5L}), \code{cf_reps}
(\code{1L}), \code{cf_seed} (\code{NULL}), \code{ps_trim} (\code{0.01}), \code{dirty_tuning} (\code{TRUE}),
\code{rho2} (\code{1}, how adversarial the confounder is) and \code{combine_method}
(\code{"median"} or \code{"mean"}).}

\item{iv_args}{Named list for \code{method = "iv"} only, with \code{parm} (any of
\code{"iv"}, \code{"fs"}, \code{"rf"}; all three by default) and \code{min} (logical,
default \code{TRUE}, passed straight to \code{\link[iv.sensemakr:sensemakr]{iv.sensemakr::sensemakr()}}).}

\item{verbose}{Logical. \code{TRUE} prints backend progress and reports dropped
incomplete rows. Default \code{FALSE}.}
}
\value{
An object of class \code{sens_res}: a list of
\describe{
\item{\code{stats}}{One tibble row per estimand, always with the same 18
columns: \code{method}, \code{estimand}, \code{term}, \code{scale}, \code{estimate},
\code{std.error}, \code{statistic}, \code{conf.low}, \code{conf.high}, \code{dof}, \code{rv_q},
\code{rv_qa}, \code{xrv_qa}, \code{r2yd_x}, \code{evalue_point}, \code{evalue_ci},
\code{tip_effect}, \code{tip_n}. Columns a backend does not produce are \code{NA}:
the robustness and partial-\eqn{R^2} columns for \code{"cox"}, the E-value
and tipping-point columns for the other three, \code{std.error} for
\code{"iv"}, and \code{rv_q} for \code{"iv"} (upstream reports only the
alpha-adjusted value). For \code{"cox"}, \code{estimate} and the confidence
limits are on the hazard-ratio scale while \code{std.error} is on the
log-hazard scale.}
\item{\code{bounds}}{Tibble of confounding scenarios, or \code{NULL} when none
was requested and for \code{method = "cox"}. Columns \code{method}, \code{estimand},
\code{bound_label}, \code{r2_treat}, \code{r2_out}, \code{adj_estimate}, \code{adj_low},
\code{adj_high}, \code{adj_conf.low}, \code{adj_conf.high}, \code{adj_se},
\code{adj_statistic}. DML and IV bounds are intervals rather than point
adjustments, so \code{adj_estimate}, \code{adj_se} and \code{adj_statistic} are \code{NA}
there.}
\item{\code{fit}}{The fitted model: \code{lm}, \code{coxph}, \code{dml} or \code{iv_fit}.}
\item{\code{sens}}{The backend sensitivity object, or \code{NULL} for \code{"cox"}.}
}
Analysis metadata is attached as \code{attr(x, "analysis")}; \code{n} counts rows
used in the fitted model after missing-value exclusion (subjects,
including censored subjects, for Cox models). Note that dplyr verbs drop
the \code{method} attribute carried by \code{stats} and \code{bounds}.
}
\description{
Single entry point that routes an observational estimate to one of four
omitted-variable-bias backends and returns their results in one shared
shape, so that a linear model, a Cox model, a double machine learning fit
and an instrumental-variable fit can be reported side by side.
}
\section{Robustness values versus E-values}{

The two headline quantities are on different scales and must not be
compared. A robustness value (\code{rv_q}, \code{rv_qa}, \code{xrv_qa}) is a partial
\eqn{R^2} in \eqn{[0, 1)}: the share of residual variation in both
treatment and outcome that a confounder would have to explain. An E-value
(\code{evalue_point}, \code{evalue_ci}) is a risk-ratio multiplier in
\eqn{[1, \infty)}: how strongly a confounder would have to be associated
with both exposure and outcome. There is no monotone mapping between them,
so they are kept in separate columns and each is \code{NA} outside its own
backend.
}

\section{Choosing rare}{

\code{evalue_args$rare} decides whether the hazard ratio is treated as a risk
ratio. The default \code{FALSE} applies the VanderWeele-Ding transform
\eqn{RR = (1 - 0.5^{\sqrt{HR}}) / (1 - 0.5^{\sqrt{1/HR}})}, which shrinks
the ratio towards the null and therefore reports a smaller, more
conservative E-value. This differs from \code{tipr}'s own default, which leaves
the hazard ratio untransformed. Set \code{rare = TRUE} only when the outcome is
genuinely rare over follow-up. The E-values match
\code{EValue::evalues.HR()} exactly on both settings; \code{EValue} itself is not
used because its \code{estimate} methods clash with \code{lava}, which
\code{dml.sensemakr} loads.
}

\examples{
\dontshow{if (requireNamespace("sensemakr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
# Linear outcome: partial R2 and robustness values
get_sens(sensemakr::darfur,
         treat = "directlyharmed", outcome = "peacefactor",
         adj_var = c("age", "farmer_dar", "herder_dar", "pastvoted",
                     "hhsize_darfur", "female", "village"),
         bench_var = "female", method = "lm",
         bench_args = list(k_treat = 1:3))
}
\dontshow{\}) # examplesIf}
\dontshow{if (requireNamespace("survival", quietly = TRUE) && requireNamespace("tipr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
# Cox / HR: tipping point and E-value
lung <- stats::na.omit(survival::lung[, c("time", "status", "sex", "age")])
lung$status <- lung$status - 1L
get_sens(lung, treat = "sex", outcome = "status", time = "time",
         adj_var = "age", method = "cox")
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=plt_sens]{plt_sens()}} for the matching plots.
}
