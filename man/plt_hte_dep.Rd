% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hte-plt.R
\name{plt_hte_dep}
\alias{plt_hte_dep}
\title{Dependence plots for heterogeneous treatment effects}
\usage{
plt_hte_dep(
  x,
  x_var = NULL,
  type = c("dep", "heat"),
  display = c("cate", "dr"),
  conf_level = 0.95,
  ylim = NULL,
  cate_smooth = 0.6,
  dr_args = list(spline_df = 2),
  pdp_args = list(grid_n = 21, max_n = 1000),
  axis_arg = list(share_y = "all"),
  title = NULL,
  save = list()
)
}
\arguments{
\item{x}{An \code{hte_res} object from \code{\link[=get_hte]{get_hte()}}.}

\item{x_var}{Covariates to draw: \code{NULL} (default) for every covariate in
the order of \code{x$importance}, a character vector of covariate names, or
\code{"fct"} / \code{"num"} for only the categorical / continuous ones. A numeric
covariate with more than 5 distinct values counts as continuous.
\code{type = "heat"} needs exactly two names. A patient missing a covariate
is left out of its panel.}

\item{type}{\code{"dep"} (default) draws one panel per covariate; \code{"heat"} draws
the partial dependence of two covariates as a heat map.}

\item{display}{Layers of a \code{"dep"} panel, any of
\describe{
\item{\code{"cate"}}{The out-of-bag CATE of every patient (grey), jittered
for a categorical covariate and smoothed by loess (span
\code{cate_smooth}) for a continuous one. Descriptive only: forest
estimates are shrunk towards the overall mean.}
\item{\code{"dr"}}{The doubly robust estimate with pointwise confidence
intervals (red): the AIPW score mean per level, or a natural spline
of the AIPW scores with HC3 errors. The strip adds \code{p_het}, the Wald
test that the CATE does not vary with the covariate. Levels with
fewer than two patients in either arm are left out, and for a
survival outcome so are levels in which an arm has no patient
followed beyond \code{time}; the spline of a continuous covariate covers
only the values that patients followed beyond \code{time} reach in both
arms. With nothing left the panel has no red layer and \code{p_het} is
\code{NA}.}
\item{\code{"pdp"}}{The partial dependence of the forest (blue): its CATE
averaged over the patients with the covariate set to each level or
grid value, other covariates as observed.}
}
Default \code{c("cate", "dr")}. Only used by \code{type = "dep"}.}

\item{conf_level}{Confidence level of the \code{"dr"} intervals. Default \code{0.95}.
Only used by \code{type = "dep"}.}

\item{ylim}{\code{NULL} (default) or two increasing numbers giving the y range
of every panel; intervals running past it are clipped.}

\item{cate_smooth}{Loess span of the grey \code{"cate"} line of a continuous
covariate, from \code{0.05} to \code{1}: smaller follows the patients more closely,
larger is smoother. Default \code{0.6}. \code{0} leaves the line out and keeps the
points. Only used by \code{type = "dep"}.}

\item{dr_args}{Named list for the \code{"dr"} layer: \code{spline_df} (default \code{2}),
the natural-spline degrees of freedom of a continuous covariate, which
also sets the degrees of freedom of its \code{p_het}: fewer is smoother, and
\code{1} fits a straight line. At the default the \code{p_het} equals the one in
\code{get_hte()$importance}. Only used by \code{type = "dep"}.}

\item{pdp_args}{Named list for the partial dependence: \code{grid_n} (default
\code{21}), the grid points of a continuous covariate, and \code{max_n} (default
\code{1000}), the most patients averaged over, taken evenly spaced so the
result does not depend on the random seed; \code{Inf} uses everyone. The
forest predicts one row per grid point and patient -- for a heat map, per
grid combination and patient -- so large values are slow.}

\item{axis_arg}{Named list with \code{share_y}: \code{"all"} (default) for one y
range across panels or \code{"none"} for a range per panel. Without \code{ylim} the
range covers the points and estimates but not the intervals, so a level
with very few patients cannot flatten every other panel. Only used by
\code{type = "dep"}.}

\item{title}{Plot title, or \code{NULL} (default).}

\item{save}{\code{NULL} or a list with \code{filename}, \code{width} and \code{height}, passed
to \code{RegR::save_plt()} for PDF output. \code{list()} and \code{NULL} skip saving; a
list naming only the file is completed with this figure's pinned size.
Do not include the \code{plot} argument; it is supplied internally.}
}
\value{
A \code{ggplot} for a single covariate or a heat map, otherwise a
patchwork with one panel per covariate, three per row. The y axis (the
fill for a heat map) is the CATE on the \code{"diff"} scale of \code{\link[=get_hte]{get_hte()}}:
the S(t) or RMST difference for a survival outcome, the risk difference
for a binary one and the mean difference otherwise. The pinned size is in
\code{attr(p, "plot_size")}. If \code{save} is non-empty, the same plot is also
written to PDF through \code{RegR::save_plt()}.
}
\description{
Draws how the conditional average treatment effect (CATE) estimated by
\code{\link[=get_hte]{get_hte()}} varies with each covariate. The layers follow
\code{StratifiedMedicine::plot_dependence()} (partial dependence and a two-way
heat map) and the panel layout of \code{MLR::plt_shp_TM()}, with a doubly robust
layer that carries valid confidence intervals.
}
\section{Three views of the same CATE}{

The layers answer different questions. The \code{"dr"} layer estimates the mean
effect among patients with a given covariate value, averaging over the
other covariates as they occur in that group, and is the only one with
confidence intervals. The \code{"pdp"} layer shows the shape the forest has
learnt when only this covariate changes and the others keep their observed
distribution. The \code{"cate"} points are the forest's own per-patient
estimates. When covariates are correlated the three can differ.
}

\examples{
\dontshow{if (requireNamespace("grf", quietly = TRUE) && requireNamespace("sandwich", quietly = TRUE) && requireNamespace("patchwork", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
set.seed(20260923)
n <- 600
d <- data.frame(age   = round(runif(n, 20, 85)),
                sex   = factor(sample(c("F", "M"), n, replace = TRUE)),
                stage = factor(sample(c("I", "II", "III"), n, replace = TRUE)))
d$z <- rbinom(n, 1, 0.5)
d$y <- rbinom(n, 1, plogis(-1 + 0.02 * (d$age - 50) +
                             d$z * (0.2 + 0.6 * (d$sex == "M"))))
res <- get_hte(d, cat_var = "z", adj_var = c("age", "sex", "stage"),
               surv = "y", grf_args = list(num.trees = 500, seed = 1))

# Every covariate, ordered by importance
plt_hte_dep(res)

# One covariate with all three layers
plt_hte_dep(res, x_var = "age", display = c("cate", "dr", "pdp"))

# Two-way partial dependence
plt_hte_dep(res, x_var = c("age", "sex"), type = "heat")
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_hte]{get_hte()}}; \code{MLR::plt_bar_per()} for \code{x$importance}.
}
