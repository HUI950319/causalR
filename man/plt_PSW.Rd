% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psw-plt.R
\name{plt_PSW}
\alias{plt_PSW}
\title{Plots for a propensity score weighting result}
\usage{
plt_PSW(
  x,
  type = c("love", "ess", "weight", "ps"),
  estimand = NULL,
  threshold = 0.1,
  bins = 40L,
  title = NULL,
  save = list()
)
}
\arguments{
\item{x}{A \code{psw_res} object from \code{\link[=get_PSW]{get_PSW()}}.}

\item{type}{Which figure to draw. \code{"love"} (default when \code{x} carries a
balance table) overlays the absolute standardised mean differences of
every weighting scheme on the unweighted ones. \code{"ess"} (the default
otherwise) compares effective sample sizes. \code{"weight"} shows the weight
distribution in each arm. \code{"ps"} mirrors the score histograms of the two
arms before and after weighting.}

\item{estimand}{Character vector selecting which estimands to draw, or
\code{NULL} (default) for all of those \code{x} holds. \code{"love"} always keeps the
unweighted series as the reference.}

\item{threshold}{Numeric, default \code{0.1}. Where \code{"love"} draws its
reference line.}

\item{bins}{Number of histogram bins for \code{"ps"}, default \code{40}.}

\item{title}{Plot title, or \code{NULL} (default) for a generated one.}

\item{save}{\code{NULL} or a list with \code{filename}, and optionally \code{width} and
\code{height}, passed to \code{RegR::save_plt()}. \code{NULL} and \code{list()} both mean no
file is written; the defaults come from the plot's own pinned size.}
}
\value{
A \code{ggplot} object, carrying its pinned size in
\code{attr(., "plot_size")}.
}
\description{
Four views of a \code{\link[=get_PSW]{get_PSW()}} result: how well each weighting scheme balances
the covariates, how much information each one costs, what the weights
themselves look like, and how much the two arms overlap on the score.
}
\examples{
\dontshow{if (requireNamespace("halfmoon", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
set.seed(20260921)
n <- 400
d <- data.frame(x1 = rnorm(n), x2 = rbinom(n, 1, 0.4), x3 = runif(n))
d$z <- rbinom(n, 1, plogis(-0.3 + 0.8 * d$x1 - 0.6 * d$x2 + 1.1 * d$x3))

res <- get_PSW(d, treat = "z", adj_var = c("x1", "x2", "x3"))
plt_PSW(res, "love")
plt_PSW(res, "ess")
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_PSW]{get_PSW()}}.
}
