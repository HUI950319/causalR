% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hte-plt.R
\name{plt_hte_cate}
\alias{plt_hte_cate}
\title{Waterfall and density plots of the CATE by subgroup}
\usage{
plt_hte_cate(
  x,
  sub_var = NULL,
  type = c("waterfall", "density"),
  show_ci = FALSE,
  conf_level = 0.95,
  overall = TRUE,
  title = NULL,
  save = list()
)
}
\arguments{
\item{x}{An \code{hte_res} object from \code{\link[=get_hte]{get_hte()}}.}

\item{sub_var}{\code{NULL} (default) for one panel of every patient, or a
character vector of categorical columns of \code{x$data}: one panel each,
three per row, coloured by level. The columns follow the rules of
\code{\link[=plt_hte_sub]{plt_hte_sub()}}; a patient missing the value is left out of that panel.}

\item{type}{\code{"waterfall"} (default) draws one bar per patient, sorted by
the CATE; \code{"density"} draws the density of the CATE per level.}

\item{show_ci}{Logical. \code{TRUE} adds grf's pointwise confidence interval of
every patient's CATE (grey), which needs a forest grown with
\code{ci.group.size} of at least 2, grf's default. Default \code{FALSE}. Only used
by \code{type = "waterfall"}.}

\item{conf_level}{Confidence level of \code{show_ci}. Default \code{0.95}. Only used
by \code{type = "waterfall"}.}

\item{overall}{Logical. \code{TRUE} (default) marks the overall ATE with a black
dashed line.}

\item{title}{Plot title, or \code{NULL} (default).}

\item{save}{\code{NULL} or a list with \code{filename}, \code{width} and \code{height}, passed
to \code{RegR::save_plt()} for PDF output. \code{list()} and \code{NULL} skip saving; a
\code{width} or \code{height} left out is taken from \code{attr(p, "plot_size")}. Do not
include the \code{plot} argument; it is supplied internally.}
}
\value{
A \code{ggplot} for one panel, otherwise a patchwork. The CATE axis is
the S(t) or RMST difference for a survival outcome, the risk difference
for a binary one and the mean difference otherwise.
\code{attr(p, "subgroup")} holds the subgroup ATEs drawn, laid out as
\code{get_hte()$subgroup} (\code{NULL} without \code{sub_var}), and
\code{attr(p, "plot_size")} a suggested \code{c(width, height)} in inches. If
\code{save} is non-empty, the plot is also written to PDF through
\code{RegR::save_plt()}.
}
\description{
Draws the conditional average treatment effect (CATE) that \code{\link[=get_hte]{get_hte()}}
estimated for every patient, after \code{StratifiedMedicine::plot_ple()}: a
waterfall of the patients sorted by their CATE, or the density of the CATE,
coloured by the levels of a subgroup variable. Dashed lines mark the
overall average treatment effect (ATE) and the subgroup ATEs, the doubly
robust estimates \code{\link[=plt_hte_sub]{plt_hte_sub()}} draws.
}
\section{Reading the plots}{

The bars and curves are the forest's out-of-bag estimates, one per patient,
on the \code{"diff"} scale of \code{\link[=get_hte]{get_hte()}}. They describe how the forest spreads
the effect and test nothing: forest estimates are shrunk towards the
overall mean, and their spread mixes real heterogeneity with estimation
noise. The coloured lines are the doubly robust subgroup ATEs, whose
intervals and \code{p_inter} \code{\link[=plt_hte_sub]{plt_hte_sub()}} shows. A level without an estimate
-- fewer than two patients in an arm, or for a survival outcome no patient
in an arm followed beyond \code{time} -- keeps its bars or curve but gets no
line, with a warning.
}

\examples{
\dontshow{if (requireNamespace("grf", quietly = TRUE) && requireNamespace("patchwork", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
set.seed(20260923)
n <- 600
d <- data.frame(age   = round(runif(n, 20, 85)),
                sex   = factor(sample(c("F", "M"), n, replace = TRUE)),
                stage = factor(sample(c("I", "II", "III"), n, replace = TRUE)))
d$z <- rbinom(n, 1, 0.5)
d$y <- rbinom(n, 1, plogis(-1 + 0.02 * (d$age - 50) +
                             d$z * (0.2 + 0.6 * (d$sex == "M"))))
res <- get_hte(d, cat_var = "z", adj_var = c("age", "sex", "stage"),
               surv = "y", grf_args = list(num.trees = 500, seed = 1))

# Patients sorted by their CATE, coloured by sex
plt_hte_cate(res, sub_var = "sex")

# Density of the CATE by sex and by stage
plt_hte_cate(res, sub_var = c("sex", "stage"), type = "density")
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=plt_hte_sub]{plt_hte_sub()}} for the subgroup ATEs with their intervals;
\code{\link[=plt_hte_dep]{plt_hte_dep()}} for how the CATE varies with a covariate.
}
