% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sens-plt.R
\name{plt_sens}
\alias{plt_sens}
\title{Plot a sensitivity analysis}
\usage{
plt_sens(
  x,
  type = c("contour", "extreme", "tip", "evalue"),
  estimand = NULL,
  threshold = 0,
  lim = NULL,
  extreme_r2 = c(1, 0.75, 0.5),
  contour_args = list(sensitivity_of = c("auto", "estimate", "t-value", "lwr", "upr"),
    n_levels = 10L, grid_n = 70L, round = 3L),
  title = NULL,
  save = list()
)
}
\arguments{
\item{x}{A \code{sens_res} object from \code{\link[=get_sens]{get_sens()}}.}

\item{type}{Which figure to draw. \code{"contour"} and \code{"extreme"} apply to
\code{method = "lm"}; \code{"contour"} alone applies to \code{"dml"} and \code{"iv"};
\code{"tip"} and \code{"evalue"} apply to \code{"cox"}. Left unset, the default is
\code{"tip"} for the Cox backend and \code{"contour"} otherwise.}

\item{estimand}{Length-1 character naming which estimand to draw when the
backend reports several (\code{"ate"}/\code{"att"}/\code{"atu"} for DML, \code{"iv"}/\code{"fs"}/
\code{"rf"} for IV). \code{NULL} (default) takes the first row of \code{x$stats}.}

\item{threshold}{Numeric value of the estimate the contour marks as the
critical line. Default \code{0}, the null.}

\item{lim}{Length-2 numeric giving the axis window: the two partial
\eqn{R^2} limits for \code{"contour"}, and the confounder-association range
for \code{"tip"} and \code{"evalue"}. \code{NULL} (default) uses the window printed by
\code{print()} on the \code{sens_res} object.}

\item{extreme_r2}{Numeric vector of outcome-side partial \eqn{R^2} values,
one curve each, for \code{type = "extreme"}. Default \code{c(1, 0.75, 0.5)}.}

\item{contour_args}{Named list forwarded to the upstream contour plot.
\describe{
\item{\code{sensitivity_of}}{Which quantity the contour is drawn for.
\code{"auto"} (default) resolves to \code{"estimate"} for \code{method = "lm"} and
\code{"lwr"} for \code{"dml"} and \code{"iv"}. Otherwise one of \code{"estimate"},
\code{"t-value"}, \code{"lwr"}, \code{"upr"}, restricted to what the backend
supports: \code{"lm"} takes all four, \code{"dml"} only \code{"lwr"}/\code{"upr"}, and
\code{"iv"} those two plus \code{"t-value"}.}
\item{\code{n_levels}}{Integer, default \code{10L}. Number of contour levels.}
\item{\code{grid_n}}{Integer, default \code{70L}. Grid resolution; DML only.}
\item{\code{round}}{Integer, default \code{3L}. Digits on the contour labels.}
}}

\item{title}{Plot title, or \code{NULL} (default) for a generated one. Used by
\code{"tip"} and \code{"evalue"} only; the wrapped upstream plots carry their own.}

\item{save}{\code{NULL} or a list with \code{filename}, \code{width} and \code{height}, passed
to \code{RegR::save_plt()} for PDF output. \code{list()} and \code{NULL} skip saving; a
list naming only the file is completed with this figure's pinned size.
Do not include the \code{plot} argument; it is supplied internally.}
}
\value{
A \code{ggplot} object, carrying its pinned size in
\code{attr(p, "plot_size")}. If \code{save} is non-empty, the same plot is also
written to PDF through \code{RegR::save_plt()}.
}
\description{
Renders the figure that matches the backend behind a \code{\link[=get_sens]{get_sens()}} result:
a partial \eqn{R^2} contour for the linear, DML and IV backends, and a
tipping-point or E-value curve for the Cox backend.
}
\section{Native figures versus wrapped ones}{

All four backends draw with base graphics upstream, and \code{gridGraphics},
which \code{\link[ggplotify:as.ggplot]{ggplotify::as.ggplot()}} relies on, cannot emulate labels on contour
lines. Wrapping therefore loses every contour level, which is the readable
content of the figure. Three of the four types avoid that:
\describe{
\item{\code{"contour"} for \code{method = "lm"}}{Drawn natively from sensemakr's
own \code{\link[sensemakr:adjusted_estimate]{sensemakr::adjusted_estimate()}}, \code{\link[sensemakr:adjusted_t]{sensemakr::adjusted_t()}} and
\code{\link[sensemakr:adjusted_ci]{sensemakr::adjusted_ci()}}, so the surface is identical to the upstream
figure but the levels stay labelled and the result is a real \code{ggplot}
that accepts further layers, scales and themes.}
\item{\code{"tip"} and \code{"evalue"}}{Drawn natively; there is no upstream
figure for either.}
\item{\code{"contour"} for \code{"dml"}/\code{"iv"}, and \code{"extreme"}}{Wrapped with
\code{\link[ggplotify:as.ggplot]{ggplotify::as.ggplot()}}, because those surfaces are not exposed as
grid functions. The result is a \code{ggplot} that saves and composes, but
its contents are a fixed grob: further layers and themes do not reach
it, and the DML and IV contours carry no level labels. The IV contour
additionally ignores \code{lim}, \code{threshold} and every \code{contour_args} field,
since \code{\link[iv.sensemakr:ovb_contour_plot]{iv.sensemakr::ovb_contour_plot()}} accepts none of them.}
}
}

\examples{
\dontshow{if (requireNamespace("sensemakr", quietly = TRUE) && requireNamespace("ggplotify", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
res <- get_sens(sensemakr::darfur,
                treat = "directlyharmed", outcome = "peacefactor",
                adj_var = c("age", "farmer_dar", "herder_dar", "pastvoted",
                            "hhsize_darfur", "female", "village"),
                bench_var = "female", method = "lm",
                bench_args = list(k_treat = 1:3))
plt_sens(res, type = "contour")
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_sens]{get_sens()}}.
}
