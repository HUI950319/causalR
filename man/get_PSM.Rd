% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psm-get.R
\name{get_PSM}
\alias{get_PSM}
\title{Propensity score matching for a binary exposure}
\usage{
get_PSM(
  data,
  treat,
  adj_var = NULL,
  ps = NULL,
  method = "nearest",
  estimand = "ATT",
  ratio = 1,
  caliper = NULL,
  replace = FALSE,
  ps_method = "glm",
  match_args = list(),
  balance = TRUE,
  ps_args = list(),
  verbose = FALSE
)
}
\arguments{
\item{data}{A data frame holding every column named below.}

\item{treat}{Length-1 character. The binary exposure column: \code{0}/\code{1},
logical, or a two-level factor or character column whose \strong{second} level
is the treated one. A character column is ordered alphabetically, so
\code{"case"} / \code{"control"} would make \code{"control"} the treated arm; convert
such a column to a factor with the control level first. The arm actually
taken as treated is recorded in \code{attr(x, "analysis")$treated} and shown
by \code{print()}. The 0/1 coding is settled here, before MatchIt sees it,
because a character exposure makes MatchIt's internal model fail and a
reversed factor silently swaps the arms; the column is returned in
\verb{$data} exactly as supplied.}

\item{adj_var}{Character vector of covariates to match on and to report
balance for.}

\item{ps}{Length-1 character or \code{NULL} (default). Column holding an
already-estimated propensity score, strictly inside (0, 1). Supplying it
skips the modelling step, so \code{ps_method} and \code{ps_args} no longer apply.}

\item{method}{Character vector of matching algorithms, any of
\code{"nearest"} (default), \code{"optimal"}, \code{"full"}, \code{"genetic"}, \code{"cem"},
\code{"exact"}, \code{"subclass"}. Each produces one weight column, one subclass
column and one \verb{$stats} row.}

\item{estimand}{Length-1 character, \code{"ATT"} (default), \code{"ATE"} or
\code{"ATC"}, applied to every selected method and validated against each.}

\item{ratio}{Number of controls matched to each treated unit. Used by
\code{"nearest"}, \code{"optimal"} and \code{"genetic"}; ignored by the others, which do
not accept it.}

\item{caliper}{\code{NULL} (default) for no caliper, or a number in \strong{standard
deviations of the propensity score}, which is the probability scale, not
the logit scale; 0.1 to 0.2 is the usual range. Austin's
0.2-standard-deviation rule is stated on the logit scale and cannot be
reproduced here, because \code{ps} must be a probability and the matching
distance is that probability. Used by \code{"nearest"}, \code{"full"} and
\code{"genetic"}; \code{"optimal"} does not accept one.}

\item{replace}{Logical, default \code{FALSE}. Allow a control to be matched more
than once. Used by \code{"nearest"} and \code{"genetic"}. With replacement the
control weights stop being 0/1 and become the number of times each
control was used.}

\item{ps_method}{Propensity model backend, passed to
\code{\link[WeightIt:weightit]{WeightIt::weightit()}} exactly as \code{\link[=get_PSW]{get_PSW()}}'s \code{method}: \code{"glm"}
(default), \code{"gbm"}, \code{"cbps"}, \code{"bart"} or \code{"super"}.}

\item{match_args}{Named list forwarded to \code{\link[MatchIt:matchit]{MatchIt::matchit()}} for every
selected method, for arguments this signature does not cover, such as
\code{exact}, \code{mahvars}, \code{m.order}, \code{discard}, \code{min.controls} or \code{subclass}.
\code{formula}, \code{data}, \code{method}, \code{estimand}, \code{distance} and \code{normalize} are
managed here and rejected if supplied.}

\item{balance}{Logical, default \code{TRUE}. Compute standardised mean
differences under every matching scheme with
\code{\link[halfmoon:check_balance]{halfmoon::check_balance()}}, on the whole cohort so that the unmatched
reference stays comparable.}

\item{ps_args}{Named list forwarded to \code{\link[WeightIt:weightit]{WeightIt::weightit()}}, for example
\code{list(link = "probit")}.}

\item{verbose}{Logical, default \code{FALSE}. Report dropped incomplete rows and
per-method matched counts.}
}
\value{
An object of class \code{psm_res}: a list of
\describe{
\item{\code{data}}{The input data, with \code{treat} exactly as supplied, plus
\code{ps}, and per method a weight column \verb{w_<method>} (\code{0} for a unit
that was not matched) and a subclass column \verb{s_<method>} (\code{NA} where
unmatched, or throughout when matching with replacement because a
reused unit can belong to multiple sets). Every input row is kept,
so the columns stay aligned and several schemes fit in one frame; the
\code{w_} prefix is what
\code{halfmoon::plot_ess(.weights = starts_with("w_"))} selects on, and
the \code{s_} prefix keeps the subclasses out of that selection.}
\item{\code{stats}}{One tibble row per method, always with the same 16
columns: \code{method}, \code{estimand}, \code{n}, \code{n_treat}, \code{n_ctrl},
\code{n_unmatched}, \code{n_discarded}, \code{pct_retained}, \code{n_pairs}, \code{ess},
\code{ess_treat}, \code{ess_ctrl}, \code{ess_pct}, \code{w_max}, \code{w_cv}, \code{smd_max},
\code{smd_over}. \code{n} and the two arm counts are of matched units;
\code{n_unmatched} and \code{n_discarded} are separate because failing to find
a partner and falling outside common support are different things.
\code{n_pairs} is the number of matched sets: pairs under 1:1 nearest
matching, strata under \code{"subclass"}, \code{"cem"} and \code{"exact"}, sets of
varying size under \code{"full"}. With replacement it counts non-empty
rows of MatchIt's matching matrix. \code{smd_max} is the largest absolute
standardised mean difference across \code{adj_var} and \code{smd_over} counts
those above 0.1, the line \code{\link[=plt_PSM]{plt_PSM()}} draws by default; both are
\code{NA} when \code{balance = FALSE}.}
\item{\code{balance}}{The \code{\link[halfmoon:check_balance]{halfmoon::check_balance()}} long table
(\code{variable}, \code{group_level}, \code{method}, \code{metric}, \code{estimate}), whose
\code{method} column holds the weight column name or \code{"observed"}. \code{NULL}
when \code{balance = FALSE}. The standardised difference is halfmoon's,
through \code{smd::smd()}: the weighted mean difference over the pooled
standard deviation of the two arms. \code{cobalt::bal.tab()} divides by
the treated arm's standard deviation for an ATT estimand by default,
so its numbers differ slightly (measured: -0.333 here against -0.357
there for the same nearest-neighbour match).}
\item{\code{fit}}{Named list of the \code{matchit} objects, one per method, for
\code{summary()}, \code{plot()}, \code{cobalt::bal.tab()} or
\code{\link[MatchIt:match_data]{MatchIt::match.data()}}, which finds the data on its own and returns
the exposure recoded to 0/1.}
}
Analysis metadata is attached as \code{attr(x, "analysis")}, including the
matchit arguments each method actually received.
}
\description{
Single entry point that fits one propensity score and hands it to any
combination of MatchIt's matching algorithms, then reports how much sample
each one keeps, how its matching weights are distributed and how well it
balances the covariates -- so that nearest-neighbour, optimal, full and
coarsened exact matching can be compared on one table rather than one at a
time.
}
\details{
\code{get_PSM()} constructs and diagnoses matched cohorts; it does not estimate
a treatment effect. Feed \code{result$data} with the weight column of your
choice to an outcome model, clustering on the matching subclass, as in
\code{lm(y ~ z, data = res$data, weights = w_nearest)} with
\code{sandwich::vcovCL(cluster = res$data$s_nearest)} for matching without
replacement. With replacement, use \code{\link[MatchIt:match_data]{MatchIt::get_matches()}} on the
corresponding \code{result$fit} entry to expand reused units into their
matching sets; account for both set membership and repeated unit IDs in
subsequent inference.
}
\section{Relation to get_PSW()}{

The two functions are duals and share their score, their \code{w_} column
convention and their diagnostics table. One name differs: in \code{\link[=get_PSW]{get_PSW()}}
\code{method} selects the \strong{propensity model}, while here \code{method} selects the
\strong{matching algorithm} -- MatchIt's own meaning -- and the propensity model
is \code{ps_method}. Matching weights are themselves inverse-probability
weights, with the modelled score replaced by the treated share inside each
matched set, so \code{w_nearest} and \code{\link[=get_PSW]{get_PSW()}}'s \code{w_att} target the same
estimand by different routes.
}

\section{Which estimand each method allows}{

Measured against MatchIt 4.7.2, not assumed. \code{"nearest"}, \code{"optimal"} and
\code{"genetic"} accept \code{"ATT"} and \code{"ATC"} only; \code{"full"}, \code{"cem"}, \code{"exact"}
and \code{"subclass"} accept \code{"ATE"} as well. Asking for an unsupported pair is
an error naming the offending method, rather than MatchIt's own message,
which does not say that another method would work.

\code{"optimal"} and \code{"full"} need \pkg{optmatch}; \code{"genetic"} needs
\pkg{Matching} and \pkg{rgenoud} and is slow. \code{"cem"} is built into
MatchIt and needs nothing.

Not every method takes every knob, and MatchIt warns once per ignored
argument, so each method is handed only what it uses: \code{ratio} reaches
\code{"nearest"}, \code{"optimal"} and \code{"genetic"}; \code{caliper} reaches \code{"nearest"},
\code{"full"} and \code{"genetic"}; \code{replace} reaches \code{"nearest"} and \code{"genetic"}.
\code{"cem"} and \code{"exact"} ignore the propensity score altogether -- they
coarsen or match on the covariates themselves -- so they are compared
alongside the others rather than sharing their score. \code{"exact"} matches
on the covariate values as they are and fails on a continuous covariate
("No exact matches were found"): give it discrete \code{adj_var} only, or use
\code{"cem"}, which coarsens first. What each method actually received is
recorded in \code{attr(x, "analysis")$specs}.

A method that fails stops the whole call with an error naming it; the
others are not reported partially.
}

\section{Weight normalisation}{

MatchIt's \code{normalize = TRUE} default rescales each arm to a mean of 1,
which removes the inverse-probability reading of the weights: at
\code{ratio = 2} the control weights would sum to twice the number of treated
units. \code{get_PSM()} fixes \code{normalize = FALSE}, so an ATT control column sums
to the number of treated units, as the textbook weight does. The effective
sample size is scale-free and is the same under either setting.
}

\examples{
\dontshow{if (requireNamespace("halfmoon", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
set.seed(20260921)
n <- 400
d <- data.frame(x1 = rnorm(n), x2 = rbinom(n, 1, 0.4), x3 = runif(n))
d$z <- rbinom(n, 1, plogis(-0.9 - 1.1 * d$x1 + 0.8 * d$x2 - 1.2 * d$x3))

res <- get_PSM(d, treat = "z", adj_var = c("x1", "x2", "x3"),
               caliper = 0.2)
res

# Nearest-neighbour against full matching, which keeps everyone
get_PSM(d, treat = "z", adj_var = c("x1", "x2", "x3"),
        method = c("nearest", "full"))$stats
}
\dontshow{\}) # examplesIf}
}
\references{
Ho DE, Imai K, King G, Stuart EA (2011). MatchIt: nonparametric
preprocessing for parametric causal inference. \emph{Journal of
Statistical Software} 42(8):1-28.

Austin PC (2011). Optimal caliper widths for propensity-score matching.
\emph{Pharmaceutical Statistics} 10(2):150-161.
}
\seealso{
\code{\link[=get_PSW]{get_PSW()}} for the weighting dual, \code{\link[=plt_PSM]{plt_PSM()}} for the plots.
}
