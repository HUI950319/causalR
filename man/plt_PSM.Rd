% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psm-plt.R
\name{plt_PSM}
\alias{plt_PSM}
\title{Plots for a propensity score matching result}
\usage{
plt_PSM(
  x,
  type = c("love", "ess", "weight", "ps"),
  method = NULL,
  threshold = 0.1,
  bins = 40L,
  title = NULL,
  save = list()
)
}
\arguments{
\item{x}{A \code{psm_res} object from \code{\link[=get_PSM]{get_PSM()}}.}

\item{type}{Which figure to draw. \code{"love"} (default when \code{x} carries a
balance table) overlays the absolute standardised mean differences of
every scheme on the unmatched ones. \code{"ess"} (the default otherwise)
compares effective sample sizes. \code{"weight"} shows the matching weights of
the matched units in each arm. \code{"ps"} mirrors the score histograms of the
whole cohort against the matched sample.}

\item{method}{Character vector selecting which matching schemes to draw, or
\code{NULL} (default) for all of those \code{x} holds. \code{"love"} always keeps the
unmatched series as the reference.}

\item{threshold}{Numeric, default \code{0.1}. Where \code{"love"} draws its
reference line.}

\item{bins}{Number of histogram bins for \code{"ps"}, default \code{40}.}

\item{title}{Plot title, or \code{NULL} (default) for a generated one.}

\item{save}{\code{NULL} or a list with \code{filename}, and optionally \code{width} and
\code{height}, passed to \code{RegR::save_plt()}. \code{NULL} and \code{list()} both mean no
file is written; the defaults come from the plot's own pinned size.}
}
\value{
A \code{ggplot} object, carrying its pinned size in
\code{attr(., "plot_size")}.
}
\description{
Four views of a \code{\link[=get_PSM]{get_PSM()}} result, mirroring \code{\link[=plt_PSW]{plt_PSW()}}: how well each
matching scheme balances the covariates, how much sample each one keeps,
what the matching weights look like, and how much of the score range
survives matching.
}
\examples{
\dontshow{if (requireNamespace("halfmoon", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
set.seed(20260921)
n <- 400
d <- data.frame(x1 = rnorm(n), x2 = rbinom(n, 1, 0.4), x3 = runif(n))
d$z <- rbinom(n, 1, plogis(-0.9 - 1.1 * d$x1 + 0.8 * d$x2 - 1.2 * d$x3))

res <- get_PSM(d, treat = "z", adj_var = c("x1", "x2", "x3"),
               caliper = 0.2)
plt_PSM(res, "love")
plt_PSM(res, "ess")
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_PSM]{get_PSM()}}, and \code{\link[=plt_PSW]{plt_PSW()}} for the weighting dual.
}
